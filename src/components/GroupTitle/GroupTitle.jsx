import styled from 'styled-components'

function GroupTitle() {
  const Title = styled.h1`
    font-family: 'Ubuntu';
    font-weight: 500;
    font-size: 2rem;
    margin: 2rem var(--global-offset);
  `

  return <Title>ИБ/б-21-3-о</Title>
}

export default GroupTitle
